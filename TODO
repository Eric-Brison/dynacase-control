
- Add missing module comparison operators in Context->getDependencies().
- Add support for <group/> nodes for parameters in Module->getParameterList().
- Install a module from any given url
- Reprise d'install d'un module installé mais en rouge/erreur
- Quand on annule la phase de param, le module apparait comme étant correctement installé et finalisé
- G�rer les acc�s concurrents aux fichiers XML dans le cas des download() lanc� en // par ex.
  (centraliser les acc�s aux XML dans Class.WIFF et mutex, ou faire des flock() quand on manipule les fichiers XML)
- impl�menter post-param
- Check for safe_mode and low max_execution_time ini settings
- Apache config should set "AllowOverride All"
- Fix all bash script for pathnames containing spaces